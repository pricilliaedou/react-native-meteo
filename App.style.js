import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    // backgroundColor: "transparent",
  },
  backgroundImage: {
    flex: 1,
    // resizeMode: "cover",
  },
});
